TODO: 
  - history, arrow key support
  - signal capture
  - pseudo terminal (PTY)

features so far:
  - unix CLI tools supported via searchingn through $PATH variable
  - piping (e.g. cat Cargo.toml | head -n 10 | grep "bin")
